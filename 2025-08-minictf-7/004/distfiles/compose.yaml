services:
  sandbox:
    build: ./sandbox
    image: tanuki-sandbox
  runner:
    build: ./runner
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - FLAG=flag{DUMMY}